@model IEnumerable<CapStoneCraftxProject.Models.Cellar>

@{
    ViewBag.Title = "Index";
}

<h2> Members Cellars  </h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CellarName)
        </th>
        <th></th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CellarName)
            </td>
            <td>
                @*@Html.DisplayFor(modelItem => item.Member.Email)*@
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |*@
                @Html.ActionLink("Personal Cellar Details", "Details", new { id = item.Id }) @*|*@
                @*@Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
            </td>

        </tr>

    }

</table>
<p>

    @Html.ActionLink("Back to Beer List", "Index", "Beers")
</p>
